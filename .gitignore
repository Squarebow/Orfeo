node_modules/
dist/
dist-electron/
out/
release/
.env
.env.local
*.log
*.tsbuildinfo
.DS_Store
Thumbs.db
.claude/
.agents/
.continue/
.vscode/
.superpowers/
.markdownlint.json
skills-lock.json
graphify-out/
.graphifyignore
assets/CLAUDE.md
CLAUDE.md

# ── Internal/planning docs — repo stays clean for public visitors ───────────
# Only docs describing the shipped app itself are tracked (ARCHITECTURE.md,
# HOW_TO_USE.md). Everything else here is local-only working material.
docs/superpowers/
docs/LR Hand rework/
docs/CSS_FINAL_SCAN.md
docs/CSS_TOKEN_CHEATSHEET.md
docs/RESEARCH_alphatab-import.md
docs/ROADMAP.md
docs/Track Color System.md
docs/UI_STRINGS_AUDIT.md
docs/tree.txt
docs/I18N_PLAN.md
docs/DESIGN_AUDIT_*.md
docs/2026-08-01-practice-mode-plan.md
docs/Final polish for public release/
docs/FONT_SIZING_HANDOFF.md
docs/PERFORMANCE_MODE_VFX_PLAN.md
docs/TOOLTIP_TOGGLE_BRIEF.md
docs/PUBLIC_RELEASE_STRATEGY.md
docs/DEBUG_TIMESTAMP.md

# ── Impeccable design skill — internal planning artifacts, not shipped docs ──
PRODUCT.md
DESIGN.md
.impeccable/

# ── CDP dev-debug helper scripts — local testing tools, not shipped app code ─
scripts/dev-debug/
.worktrees/

# ── Chord-detection library audit — dev tooling + generated reports ─────────
scripts/chord-audit/

# ── Playwright MCP — local browser-testing artifacts, not shipped app code ──
.playwright-mcp/
docs/DESIGN-arp-pattern-mining.md

# ── Reusable local tooling instructions (e.g. media-conversion recipes) —
# not part of the shipped app or its docs ──────────────────────────────────
docs/Tools/

# ── docs/Images — working screenshots + SVG icon sources for the docs,
# prepared locally, never committed (docs render fine without them) ────────
docs/Images/

# ── docs/Chords display — chord-detection testing CSVs (exported table vs.
# hand-edited/annotated copy), local test material not shipped docs ────────
docs/Chords display/

# ── docs/Test Files — ground-truth chord-detection CSVs (before/after app
# output vs. hand-verified correct chords) the user provides per song for
# the algorithm-fixing workflow; local test material, not shipped docs ─────
docs/Test Files/

# ── docs/Chord Detection Status.md — internal working notes (current stage,
# open items, gotchas, reverted attempts) for the ongoing chord-detection
# effort; not a shipped doc ─────────────────────────────────────────────────
docs/Chord Detection Status.md

# ── docs/Chord Engine Dual-Mode Plan.md — internal technical reference (for
# the assistant, across sessions) for the Safe/Progressive dual chord-
# reading-engine initiative; not a shipped doc ──────────────────────────────
docs/Chord Engine Dual-Mode Plan.md

# ── public/images — website screenshots and reference shots, prepared here
# but hosted on orfeo.cc, not committed (this folder also ships inside the
# packaged app, so it stays empty of heavy assets) ─────────────────────────
public/images/
